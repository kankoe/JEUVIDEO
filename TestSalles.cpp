#define DOCTEST_CONFIG_IMPLEMENT_WITH_MAIN
#include "doctest.h"
#include "salles.h"

/*TEST_CASE("Test de la classe Salles") {
    SUBCASE("Test du constructeur par défaut") {
        salles salle;
        REQUIRE_EQ(salle.abcisse(), 0);
        REQUIRE_EQ(salle.ordonner(), 0);
        REQUIRE_EQ(salle.height(), 0);
        REQUIRE_EQ(salle.width(), 0);

    }

    SUBCASE("Test du constructeur avec des valeurs spécifiées") {
        salles salle(5, 10, 20, 15);
        REQUIRE_EQ(salle.abcisse(), 10);
        REQUIRE_EQ(salle.ordonner(), 5);
        REQUIRE_EQ(salle.height(), 20);
        REQUIRE_EQ(salle.width(), 15);
    }

    SUBCASE("Test des setters") {
        salles salle;
        salle.setabcisse(3);
        salle.setordonner(7);
        salle.setwalkable(false);
        salle.setcaractere('X');

        REQUIRE_EQ(salle.abcisse(), 3);
        REQUIRE_EQ(salle.ordonner(), 7);
        REQUIRE_EQ(salle.height(), 0);
        REQUIRE_EQ(salle.width(), 0);
        REQUIRE_EQ(salle.walkable(), false);
        REQUIRE_EQ(salle.caractere(), 'X');
    }
}*/
